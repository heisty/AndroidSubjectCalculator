import { StyleSheet, Dimensions } from 'react-native';

const styles = StyleSheet.create({
    card: {
    	width: Dimensions.get('window').width,
    	height: 80,
    	backgroundColor: '#C0004B',
    }
});

module.exports = styles;